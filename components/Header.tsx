import React from 'react';

type Props = {}

const Header = ({}: Props) => {
    return (
        <header>
            I am the header
        </header>
    );
};

export default Header;
// by Rokas with ❤️
