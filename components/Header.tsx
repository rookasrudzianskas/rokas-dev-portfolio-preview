import React from 'react';

type Props = {}

const Header = ({}: Props) => {
    return (
        <div>
            I am the header
        </div>
    );
};

export default Header;
// by Rokas with ❤️
